---
tipo: prompt
tags: [agentes-ia, gestão-conhecimento, produtividade]
uso: [produtividade, gestão-conhecimento]
destino: B
---

T: Organização .claude Padrão de Qualidade
F: [Arsenal Pira Labs, fonte não recuperada]
TP: configuração
FN: Define a estrutura base da pasta `.claude` para agentes, comandos, hooks, regras, skills e contexto raiz. Use quando precisar preparar ou auditar a organização padrão de um projeto Claude Code.
PERSONA: —
ETAPAS: —
REGRAS: Estruturar a pasta `.claude` com `agents/`, `commands/`, `hooks/`, `rules/`, `skills/`, `settings.json` e `CLAUDE.md`.
SC: —
OF: —
CP: —
OT: Estrutura de diretórios `.claude`.
SP: Subpartes declaradas no material: `agents/`, `commands/`, `hooks/`, `rules/`, `skills/`, `settings.json`, `CLAUDE.md`.
